import { BrowserRouter as Router, Route, Routes } from 'react-router-dom';

function routes() {
  return (
    <Router>
      <div>
        <div>
          <Routes></Routes>
        </div>
      </div>
    </Router>
  );
}

export default routes;
